# Function calling

This example demonstrates how to use function/tool calling with the Azure OpenAI Chat Completions API. It defines a weather function tool, sends a request that triggers the tool call, and processes the model's response to extract the function name and arguments.
