openapi: 3.0.0
info:
  title: Azure AI Foundry Models Service
  license:
    name: MIT
    url: https://github.com/openai/openai-openapi/blob/master/LICENSE
  version: v1
tags:
- name: Batch
- name: Chat
- name: Completions
- name: Containers
- name: Conversations
- name: Evals
- name: Files
- name: Embeddings
- name: Fine-tuning
- name: Models
- name: Realtime
- name: Responses
- name: Threads
- name: Vector Stores
paths:
  /chat/completions:
    post:
      operationId: createChatCompletion
      description: Creates a chat completion.
      parameters:
      - name: api-version
        in: query
        required: false
        description: |-
          The explicit Azure AI Foundry Models API version to use for this request.
          `v1` if not otherwise specified.
        schema:
          $ref: '#/components/schemas/AzureAIFoundryModelsApiVersion'
          default: v1
      responses:
        '200':
          description: The request has succeeded.
          headers:
            apim-request-id:
              required: false
              description: A request ID used for troubleshooting purposes.
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InlineResponse200'
        default:
          description: An unexpected error response.
          headers:
            apim-request-id:
              required: false
              description: A request ID used for troubleshooting purposes.
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  code:
                    type: string
                    nullable: true
                  message:
                    type: string
                  param:
                    type: string
                    nullable: true
                  type:
                    type: string
                  inner_error: {}
                required:
                - code
                - message
      tags:
      - Chat
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChatCompletionsBody'
      x-ms-examples:
        Create a chat completion:
          $ref: ./examples/chat_completions.yaml
security:
- ApiKeyAuth: []
- OAuth2Auth:
  - https://cognitiveservices.azure.com/.default
components:
  schemas:
    AzureAIFoundryModelsApiVersion:
      type: string
      enum:
      - v1
      - preview
    AzureContentFilterBlocklistResult:
      type: object
      required:
      - filtered
      properties:
        filtered:
          type: boolean
          description: A value indicating whether any of the detailed blocklists resulted in a filtering action.
        details:
          type: array
          items:
            $ref: '#/components/schemas/AzureContentFilterBlocklistResultDetails'
          description: The pairs of individual blocklist IDs and whether they resulted in a filtering action.
      description: A collection of true/false filtering results for configured custom blocklists.
    AzureContentFilterCompletionTextSpan:
      type: object
      required:
      - completion_start_offset
      - completion_end_offset
      properties:
        completion_start_offset:
          type: integer
          format: int32
          description: Offset of the UTF32 code point which begins the span.
        completion_end_offset:
          type: integer
          format: int32
          description: Offset of the first UTF32 code point which is excluded from the span. This field is always equal to completion_start_offset for empty spans. This field is always larger than completion_start_offset for non-empty spans.
      description: A representation of a span of completion text as used by Azure OpenAI content filter results.
    AzureContentFilterCompletionTextSpanDetectionResult:
      type: object
      required:
      - filtered
      - detected
      - details
      properties:
        filtered:
          type: boolean
          description: Whether the content detection resulted in a content filtering action.
        detected:
          type: boolean
          description: Whether the labeled content category was detected in the content.
        details:
          type: array
          items:
            $ref: '#/components/schemas/AzureContentFilterCompletionTextSpan'
          description: Detailed information about the detected completion text spans.
    AzureContentFilterCustomTopicResult:
      type: object
      required:
      - filtered
      properties:
        filtered:
          type: boolean
          description: A value indicating whether any of the detailed topics resulted in a filtering action.
        details:
          type: array
          items:
            $ref: '#/components/schemas/AzureContentFilterCustomTopicResultDetails'
          description: The pairs of individual topic IDs and whether they are detected.
      description: A collection of true/false filtering results for configured custom topics.
    AzureContentFilterDetectionResult:
      type: object
      required:
      - filtered
      - detected
      properties:
        filtered:
          type: boolean
          description: Whether the content detection resulted in a content filtering action.
        detected:
          type: boolean
          description: Whether the labeled content category was detected in the content.
      description: |-
        A labeled content filter result item that indicates whether the content was detected and whether the content was
        filtered.
    AzureContentFilterPersonallyIdentifiableInformationResult:
      type: object
      properties:
        redacted_text:
          type: string
          description: The redacted text with PII information removed or masked.
        sub_categories:
          type: array
          items:
            $ref: '#/components/schemas/AzurePiiSubCategoryResult'
          description: Detailed results for individual PIIHarmSubCategory(s).
      allOf:
      - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
      description: A content filter detection result for Personally Identifiable Information that includes harm extensions.
    AzureContentFilterResultForChoice:
      type: object
      properties:
        sexual:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category for language related to anatomical organs and genitals, romantic relationships, acts
            portrayed in erotic or affectionate terms, pregnancy, physical sexual acts, including those portrayed as an
            assault or a forced sexual violent act against one's will, prostitution, pornography, and abuse.
        hate:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category that can refer to any content that attacks or uses pejorative or discriminatory
            language with reference to a person or identity group based on certain differentiating attributes of these groups
            including but not limited to race, ethnicity, nationality, gender identity and expression, sexual orientation,
            religion, immigration status, ability status, personal appearance, and body size.
        violence:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category for language related to physical actions intended to hurt, injure, damage, or kill
            someone or something; describes weapons, guns and related entities, such as manufactures, associations,
            legislation, and so on.
        self_harm:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category that describes language related to physical actions intended to purposely hurt, injure,
            damage one's body or kill oneself.
        profanity:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
          description: |-
            A detection result that identifies whether crude, vulgar, or otherwise objection language is present in the
            content.
        custom_blocklists:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterBlocklistResult'
          description: A collection of binary filtering outcomes for configured custom blocklists.
        custom_topics:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterCustomTopicResult'
          description: A collection of binary filtering outcomes for configured custom topics.
        error:
          $ref: '#/components/schemas/AzureContentFilterResultForChoiceError'
        protected_material_text:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
          description: A detection result that describes a match against text protected under copyright or other status.
        protected_material_code:
          $ref: '#/components/schemas/AzureContentFilterResultForChoiceProtectedMaterialCode'
        ungrounded_material:
          $ref: '#/components/schemas/AzureContentFilterCompletionTextSpanDetectionResult'
        personally_identifiable_information:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterPersonallyIdentifiableInformationResult'
          description: A detection result that describes matches against Personal Identifiable Information with configurable subcategories.
      description: A content filter result for a single response item produced by a generative AI system.
    AzureContentFilterResultForPrompt:
      type: object
      properties:
        prompt_index:
          type: integer
          format: int32
          description: The index of the input prompt associated with the accompanying content filter result categories.
        content_filter_results:
          $ref: '#/components/schemas/AzureContentFilterResultForPromptContentFilterResults'
      description: A content filter result associated with a single input prompt item into a generative AI system.
    AzureContentFilterSeverityResult:
      type: object
      required:
      - filtered
      - severity
      properties:
        filtered:
          type: boolean
          description: Whether the content severity resulted in a content filtering action.
        severity:
          type: string
          enum:
          - safe
          - low
          - medium
          - high
          description: The labeled severity of the content.
      description: |-
        A labeled content filter result item that indicates whether the content was filtered and what the qualitative
        severity level of the content was, as evaluated against content filter configuration for the category.
    AzurePiiSubCategoryResult:
      type: object
      required:
      - sub_category
      - filtered
      - detected
      - redacted
      properties:
        sub_category:
          type: string
          description: The PIIHarmSubCategory that was evaluated.
        filtered:
          type: boolean
          description: Whether the content detection resulted in a content filtering action for this subcategory.
        detected:
          type: boolean
          description: Whether the labeled content subcategory was detected in the content.
        redacted:
          type: boolean
          description: Whether the content was redacted for this subcategory.
      description: Result details for individual PIIHarmSubCategory(s).
    AzureUserSecurityContext:
      type: object
      properties:
        application_name:
          type: string
          description: The name of the application. Sensitive personal information should not be included in this field.
        end_user_id:
          type: string
          description: This identifier is the Microsoft Entra ID (formerly Azure Active Directory) user object ID used to authenticate end-users within the generative AI application. Sensitive personal information should not be included in this field.
        end_user_tenant_id:
          type: string
          description: The Microsoft 365 tenant ID the end user belongs to. It's required when the generative AI application is multitenant.
        source_ip:
          type: string
          description: Captures the original client's IP address.
      description: User security context contains several parameters that describe the application itself, and the end user that interacts with the application. These fields assist your security operations teams to investigate and mitigate security incidents by providing a comprehensive approach to protecting your AI applications. [Learn more](https://aka.ms/TP4AI/Documentation/EndUserContext) about protecting AI applications using Microsoft Defender for Cloud.
    OpenAIChatCompletionAllowedTools:
      type: object
      required:
      - mode
      - tools
      properties:
        mode:
          type: string
          enum:
          - auto
          - required
          description: |-
            Constrains the tools available to the model to a pre-defined set.
              `auto` allows the model to pick from among the allowed tools and generate a
              message.
              `required` requires the model to call one or more of the allowed tools.
        tools:
          type: array
          items:
            type: object
          description: |-
            A list of tool definitions that the model should be allowed to call.
              For the Chat Completions API, the list of tool definitions might look like:
              ```json
              [
                { "type": "function", "function": { "name": "get_weather" } },
                { "type": "function", "function": { "name": "get_time" } }
              ]
              ```
      description: Constrains the tools available to the model to a pre-defined set.
      title: Allowed tools
    OpenAIChatCompletionAllowedToolsChoice:
      type: object
      required:
      - type
      - allowed_tools
      properties:
        type:
          type: string
          enum:
          - allowed_tools
          description: Allowed tool configuration type. Always `allowed_tools`.
          x-stainless-const: true
        allowed_tools:
          $ref: '#/components/schemas/OpenAIChatCompletionAllowedTools'
      description: Constrains the tools available to the model to a pre-defined set.
      title: Allowed tools
    OpenAIChatCompletionFunctionCallOption:
      type: object
      required:
      - name
      properties:
        name:
          type: string
          description: The name of the function to call.
      description: 'Specifying a particular function via `{"name": "my_function"}` forces the model to call that function.'
    OpenAIChatCompletionFunctions:
      type: object
      required:
      - name
      properties:
        description:
          type: string
          description: A description of what the function does, used by the model to choose when and how to call the function.
        name:
          type: string
          description: The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64.
        parameters:
          allOf:
          - $ref: '#/components/schemas/OpenAIFunctionParameters'
          description: |-
            The parameters the functions accepts, described as a JSON Schema object.
            See the [JSON Schema reference](https://json-schema.org/understanding-json-schema/)
            for documentation about the format.

            Omitting `parameters` defines a function with an empty parameter list.
      deprecated: true
    OpenAIChatCompletionMessageCustomToolCall:
      type: object
      required:
      - id
      - type
      - custom
      properties:
        id:
          type: string
          description: The ID of the tool call.
        type:
          type: string
          enum:
          - custom
          description: The type of the tool. Always `custom`.
          x-stainless-const: true
        custom:
          allOf:
          - $ref: '#/components/schemas/OpenAIChatCompletionMessageCustomToolCallCustom'
          description: The custom tool that the model called.
      description: A call to a custom tool created by the model.
      title: Custom tool call
    OpenAIChatCompletionMessageCustomToolCallCustom:
      type: object
      required:
      - name
      - input
      properties:
        name:
          type: string
        input:
          type: string
    OpenAIChatCompletionMessageToolCall:
      type: object
      required:
      - id
      - type
      - function
      properties:
        id:
          type: string
          description: The ID of the tool call.
        type:
          type: string
          enum:
          - function
          description: The type of the tool. Currently, only `function` is supported.
          x-stainless-const: true
        function:
          allOf:
          - $ref: '#/components/schemas/OpenAIChatCompletionMessageToolCallFunction'
          description: The function that the model called.
      description: A call to a function tool created by the model.
      title: Function tool call
    OpenAIChatCompletionMessageToolCallChunk:
      type: object
      required:
      - index
      properties:
        index:
          type: integer
        id:
          type: string
          description: The ID of the tool call.
        type:
          type: string
          enum:
          - function
          description: The type of the tool. Currently, only `function` is supported.
          x-stainless-const: true
        function:
          $ref: '#/components/schemas/OpenAIChatCompletionMessageToolCallChunkFunction'
    OpenAIChatCompletionMessageToolCallChunkFunction:
      type: object
      properties:
        name:
          type: string
        arguments:
          type: string
    OpenAIChatCompletionMessageToolCallFunction:
      type: object
      required:
      - name
      - arguments
      properties:
        name:
          type: string
        arguments:
          type: string
    OpenAIChatCompletionMessageToolCallsItem:
      type: array
      items:
        anyOf:
        - $ref: '#/components/schemas/OpenAIChatCompletionMessageToolCall'
        - $ref: '#/components/schemas/OpenAIChatCompletionMessageCustomToolCall'
      description: The tool calls generated by the model, such as function calls.
    OpenAIChatCompletionNamedToolChoice:
      type: object
      required:
      - type
      - function
      properties:
        type:
          type: string
          enum:
          - function
          description: For function calling, the type is always `function`.
          x-stainless-const: true
        function:
          $ref: '#/components/schemas/OpenAIChatCompletionNamedToolChoiceFunction'
      description: Specifies a tool the model should use. Use to force the model to call a specific function.
      title: Function tool choice
    OpenAIChatCompletionNamedToolChoiceCustom:
      type: object
      required:
      - type
      - custom
      properties:
        type:
          type: string
          enum:
          - custom
          description: For custom tool calling, the type is always `custom`.
          x-stainless-const: true
        custom:
          $ref: '#/components/schemas/OpenAIChatCompletionNamedToolChoiceCustomCustom'
      description: Specifies a tool the model should use. Use to force the model to call a specific custom tool.
      title: Custom tool choice
    OpenAIChatCompletionNamedToolChoiceCustomCustom:
      type: object
      required:
      - name
      properties:
        name:
          type: string
    OpenAIChatCompletionNamedToolChoiceFunction:
      type: object
      required:
      - name
      properties:
        name:
          type: string
    OpenAIChatCompletionRequestMessage:
      type: object
      required:
      - role
      properties:
        role:
          $ref: '#/components/schemas/OpenAIChatCompletionRequestMessageType'
      discriminator:
        propertyName: role
        mapping:
          assistant: '#/components/schemas/OpenAI.ChatCompletionRequestAssistantMessage'
          developer: '#/components/schemas/OpenAI.ChatCompletionRequestDeveloperMessage'
          function: '#/components/schemas/OpenAI.ChatCompletionRequestFunctionMessage'
          system: '#/components/schemas/OpenAI.ChatCompletionRequestSystemMessage'
          user: '#/components/schemas/OpenAI.ChatCompletionRequestUserMessage'
          tool: '#/components/schemas/OpenAI.ChatCompletionRequestToolMessage'
    OpenAIChatCompletionRequestMessageContentPartText:
      type: object
      required:
      - type
      - text
      properties:
        type:
          type: string
          enum:
          - text
          description: The type of the content part.
          x-stainless-const: true
        text:
          type: string
          description: The text content.
      description: ''
      title: Text content part
    OpenAIChatCompletionRequestMessageType:
      anyOf:
      - type: string
      - type: string
        enum:
        - developer
        - system
        - user
        - assistant
        - tool
        - function
    OpenAIChatCompletionResponseMessage:
      type: object
      required:
      - content
      - refusal
      - role
      properties:
        content:
          type: string
          nullable: true
        refusal:
          type: string
          nullable: true
        tool_calls:
          $ref: '#/components/schemas/OpenAIChatCompletionMessageToolCallsItem'
        annotations:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionResponseMessageAnnotations'
          description: |-
            Annotations for the message, when applicable, as when using the
              [web search tool](/docs/guides/tools-web-search?api-mode=chat).
        role:
          type: string
          enum:
          - assistant
          description: The role of the author of this message.
          x-stainless-const: true
        function_call:
          allOf:
          - $ref: '#/components/schemas/OpenAIChatCompletionResponseMessageFunctionCall'
          description: Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
          deprecated: true
        audio:
          $ref: '#/components/schemas/OpenAIChatCompletionResponseMessageAudio'
          nullable: true
        reasoning_content:
          type: string
          description: An Azure-specific extension property containing generated reasoning content from supported models.
      description: |-
        If the audio output modality is requested, this object contains data
        about the audio response from the model.
    OpenAIChatCompletionResponseMessageAnnotations:
      type: object
      required:
      - type
      - url_citation
      properties:
        type:
          type: string
          enum:
          - url_citation
          x-stainless-const: true
        url_citation:
          $ref: '#/components/schemas/OpenAIChatCompletionResponseMessageAnnotationsUrlCitation'
    OpenAIChatCompletionResponseMessageAnnotationsUrlCitation:
      type: object
      required:
      - end_index
      - start_index
      - url
      - title
      properties:
        end_index:
          type: integer
        start_index:
          type: integer
        url:
          type: string
          format: uri
        title:
          type: string
    OpenAIChatCompletionResponseMessageAudio:
      type: object
      required:
      - id
      - expires_at
      - data
      - transcript
      properties:
        id:
          type: string
        expires_at:
          type: integer
          format: unixtime
        data:
          type: string
        transcript:
          type: string
    OpenAIChatCompletionResponseMessageFunctionCall:
      type: object
      required:
      - arguments
      - name
      properties:
        arguments:
          type: string
        name:
          type: string
    OpenAIChatCompletionStreamOptions:
      type: object
      properties:
        include_usage:
          type: boolean
          description: |-
            If set, an additional chunk will be streamed before the `data: [DONE]`
              message. The `usage` field on this chunk shows the token usage statistics
              for the entire request, and the `choices` field will always be an empty
              array.
              All other chunks will also include a `usage` field, but with a null
              value. **NOTE:** If the stream is interrupted, you may not receive the
              final usage chunk which contains the total token usage for the request.
        include_obfuscation:
          type: boolean
          description: |-
            When true, stream obfuscation will be enabled. Stream obfuscation adds
              random characters to an `obfuscation` field on streaming delta events to
              normalize payload sizes as a mitigation to certain side-channel attacks.
              These obfuscation fields are included by default, but add a small amount
              of overhead to the data stream. You can set `include_obfuscation` to
              false to optimize for bandwidth if you trust the network links between
              your application and the OpenAI API.
      description: 'Options for streaming response. Only set this when you set `stream: true`.'
    OpenAIChatCompletionStreamResponseDelta:
      type: object
      properties:
        content:
          type: string
          nullable: true
        function_call:
          allOf:
          - $ref: '#/components/schemas/OpenAIChatCompletionStreamResponseDeltaFunctionCall'
          description: Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.
          deprecated: true
        tool_calls:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionMessageToolCallChunk'
        role:
          type: string
          enum:
          - developer
          - system
          - user
          - assistant
          - tool
          description: The role of the author of this message.
        refusal:
          type: string
          nullable: true
        reasoning_content:
          type: string
          description: An Azure-specific extension property containing generated reasoning content from supported models.
      description: A chat completion delta generated by streamed model responses.
    OpenAIChatCompletionStreamResponseDeltaFunctionCall:
      type: object
      properties:
        arguments:
          type: string
        name:
          type: string
    OpenAIChatCompletionTokenLogprob:
      type: object
      required:
      - token
      - logprob
      - bytes
      - top_logprobs
      properties:
        token:
          type: string
          description: The token.
        logprob:
          type: number
          description: The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely.
        bytes:
          type: array
          items:
            type: integer
          nullable: true
        top_logprobs:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionTokenLogprobTopLogprobs'
          description: List of the most likely tokens and their log probability, at this token position. The number of entries may be fewer than the requested `top_logprobs`.
    OpenAIChatCompletionTokenLogprobTopLogprobs:
      type: object
      required:
      - token
      - logprob
      - bytes
      properties:
        token:
          type: string
        logprob:
          type: number
        bytes:
          type: array
          items:
            type: integer
          nullable: true
    OpenAIChatCompletionTool:
      type: object
      required:
      - type
      - function
      properties:
        type:
          type: string
          enum:
          - function
          description: The type of the tool. Currently, only `function` is supported.
          x-stainless-const: true
        function:
          $ref: '#/components/schemas/OpenAIFunctionObject'
      description: A function tool that can be used to generate a response.
      title: Function tool
    OpenAIChatCompletionToolChoiceOption:
      oneOf:
      - type: string
        enum:
        - none
        - auto
        - required
      - $ref: '#/components/schemas/OpenAIChatCompletionAllowedToolsChoice'
      - $ref: '#/components/schemas/OpenAIChatCompletionNamedToolChoice'
      - $ref: '#/components/schemas/OpenAIChatCompletionNamedToolChoiceCustom'
      description: |-
        Controls which (if any) tool is called by the model.
        `none` means the model will not call any tool and instead generates a message.
        `auto` means the model can pick between generating a message or calling one or more tools.
        `required` means the model must call one or more tools.
        Specifying a particular tool via `{"type": "function", "function": {"name": "my_function"}}` forces the model to call that tool.
        `none` is the default when no tools are present. `auto` is the default if tools are present.
    OpenAICompletionUsage:
      type: object
      required:
      - completion_tokens
      - prompt_tokens
      - total_tokens
      properties:
        completion_tokens:
          type: integer
          description: Number of tokens in the generated completion.
        prompt_tokens:
          type: integer
          description: Number of tokens in the prompt.
        total_tokens:
          type: integer
          description: Total number of tokens used in the request (prompt + completion).
        completion_tokens_details:
          allOf:
          - $ref: '#/components/schemas/OpenAICompletionUsageCompletionTokensDetails'
          description: Breakdown of tokens used in a completion.
        prompt_tokens_details:
          allOf:
          - $ref: '#/components/schemas/OpenAICompletionUsagePromptTokensDetails'
          description: Breakdown of tokens used in the prompt.
      description: Usage statistics for the completion request.
    OpenAICompletionUsageCompletionTokensDetails:
      type: object
      properties:
        accepted_prediction_tokens:
          type: integer
        audio_tokens:
          type: integer
        reasoning_tokens:
          type: integer
        rejected_prediction_tokens:
          type: integer
    OpenAICompletionUsagePromptTokensDetails:
      type: object
      properties:
        audio_tokens:
          type: integer
        cached_tokens:
          type: integer
    OpenAICreateChatCompletionRequestAudio:
      type: object
      required:
      - voice
      - format
      properties:
        voice:
          $ref: '#/components/schemas/OpenAIVoiceIdsOrCustomVoice'
        format:
          type: string
          enum:
          - wav
          - aac
          - mp3
          - flac
          - opus
          - pcm16
    OpenAICreateChatCompletionRequestResponseFormat:
      type: object
      required:
      - type
      properties:
        type:
          $ref: '#/components/schemas/OpenAICreateChatCompletionRequestResponseFormatType'
      discriminator:
        propertyName: type
        mapping:
          json_schema: '#/components/schemas/OpenAI.ResponseFormatJsonSchema'
          text: '#/components/schemas/OpenAI.CreateChatCompletionRequestResponseFormatResponseFormatText'
          json_object: '#/components/schemas/OpenAI.CreateChatCompletionRequestResponseFormatResponseFormatJsonObject'
      description: |-
        An object specifying the format that the model must output.
        Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
        Structured Outputs which ensures the model will match your supplied JSON
        schema. Learn more in the [Structured Outputs
        guide](/docs/guides/structured-outputs).
        Setting to `{ "type": "json_object" }` enables the older JSON mode, which
        ensures the message the model generates is valid JSON. Using `json_schema`
        is preferred for models that support it.
    OpenAICreateChatCompletionRequestResponseFormatType:
      anyOf:
      - type: string
      - type: string
        enum:
        - text
        - json_schema
        - json_object
    OpenAICreateChatCompletionResponseChoices:
      type: object
      required:
      - finish_reason
      - index
      - message
      - logprobs
      properties:
        finish_reason:
          type: string
          enum:
          - stop
          - length
          - tool_calls
          - content_filter
          - function_call
        index:
          type: integer
        message:
          $ref: '#/components/schemas/OpenAIChatCompletionResponseMessage'
        logprobs:
          allOf:
          - $ref: '#/components/schemas/OpenAICreateChatCompletionResponseChoicesLogprobs'
          nullable: true
        content_filter_results:
          $ref: '#/components/schemas/AzureContentFilterResultForChoice'
    OpenAICreateChatCompletionResponseChoicesLogprobs:
      type: object
      required:
      - content
      - refusal
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionTokenLogprob'
          nullable: true
        refusal:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionTokenLogprob'
          nullable: true
    OpenAICreateChatCompletionStreamResponseChoices:
      type: object
      required:
      - delta
      - finish_reason
      - index
      properties:
        delta:
          $ref: '#/components/schemas/OpenAIChatCompletionStreamResponseDelta'
        logprobs:
          $ref: '#/components/schemas/OpenAICreateChatCompletionStreamResponseChoicesLogprobs'
          nullable: true
        finish_reason:
          type: string
          enum:
          - stop
          - length
          - tool_calls
          - content_filter
          - function_call
          nullable: true
        index:
          type: integer
    OpenAICreateChatCompletionStreamResponseChoicesLogprobs:
      type: object
      required:
      - content
      - refusal
      properties:
        content:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionTokenLogprob'
          nullable: true
        refusal:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionTokenLogprob'
          nullable: true
    OpenAICustomToolChatCompletions:
      type: object
      required:
      - type
      - custom
      properties:
        type:
          type: string
          enum:
          - custom
          description: The type of the custom tool. Always `custom`.
          x-stainless-const: true
        custom:
          allOf:
          - $ref: '#/components/schemas/OpenAICustomToolChatCompletionsCustom'
          description: Properties of the custom tool.
          title: Custom tool properties
      description: A custom tool that processes input using a specified format.
      title: Custom tool
    OpenAICustomToolChatCompletionsCustom:
      type: object
      required:
      - name
      properties:
        name:
          type: string
        description:
          type: string
        format:
          oneOf:
          - $ref: '#/components/schemas/OpenAICustomToolChatCompletionsCustomFormatText'
          - $ref: '#/components/schemas/OpenAICustomToolChatCompletionsCustomFormatGrammar'
    OpenAICustomToolChatCompletionsCustomFormatGrammar:
      type: object
      required:
      - type
      - grammar
      properties:
        type:
          type: string
          enum:
          - grammar
          x-stainless-const: true
        grammar:
          allOf:
          - $ref: '#/components/schemas/OpenAICustomToolChatCompletionsCustomFormatGrammarGrammar'
          title: Grammar format
    OpenAICustomToolChatCompletionsCustomFormatGrammarGrammar:
      type: object
      required:
      - definition
      - syntax
      properties:
        definition:
          type: string
        syntax:
          type: string
          enum:
          - lark
          - regex
    OpenAICustomToolChatCompletionsCustomFormatText:
      type: object
      required:
      - type
      properties:
        type:
          type: string
          enum:
          - text
          x-stainless-const: true
    OpenAIFunctionObject:
      type: object
      required:
      - name
      properties:
        description:
          type: string
          description: A description of what the function does, used by the model to choose when and how to call the function.
        name:
          type: string
          description: The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64.
        parameters:
          $ref: '#/components/schemas/OpenAIFunctionParameters'
        strict:
          type: boolean
          nullable: true
    OpenAIFunctionParameters:
      type: object
      description: |-
        The parameters the functions accepts, described as a JSON Schema object. See the [guide](/docs/guides/function-calling) for examples, and the [JSON Schema reference](https://json-schema.org/understanding-json-schema/) for documentation about the format.
        Omitting `parameters` defines a function with an empty parameter list.
    OpenAIMetadata:
      type: object
      description: |-
        Set of 16 key-value pairs that can be attached to an object. This can be
        useful for storing additional information about the object in a structured
        format, and querying for objects via API or the dashboard.
        Keys are strings with a maximum length of 64 characters. Values are strings
        with a maximum length of 512 characters.
      x-oaiTypeLabel: map
    OpenAIPredictionContent:
      type: object
      required:
      - type
      - content
      properties:
        type:
          type: string
          enum:
          - content
          description: |-
            The type of the predicted content you want to provide. This type is
              currently always `content`.
          x-stainless-const: true
        content:
          oneOf:
          - type: string
          - type: array
            items:
              $ref: '#/components/schemas/OpenAIChatCompletionRequestMessageContentPartText'
          description: |-
            The content that should be matched when generating a model response.
              If generated tokens would match this content, the entire model response
              can be returned much more quickly.
      description: |-
        Static predicted output content, such as the content of a text file that is
        being regenerated.
      title: Static Content
    OpenAIReasoningEffort:
      description: |-
        Constrains effort on reasoning for
        [reasoning models](https://platform.openai.com/docs/guides/reasoning).
        Currently supported values are `none`, `minimal`, `low`, `medium`, `high`, and `xhigh`. Reducing
        reasoning effort can result in faster responses and fewer tokens used
        on reasoning in a response.
        - `gpt-5.1` defaults to `none`, which does not perform reasoning. The supported reasoning values for `gpt-5.1` are `none`, `low`, `medium`, and `high`. Tool calls are supported for all reasoning values in gpt-5.1.
        - All models before `gpt-5.1` default to `medium` reasoning effort, and do not support `none`.
        - The `gpt-5-pro` model defaults to (and only supports) `high` reasoning effort.
        - `xhigh` is supported for all models after `gpt-5.1-codex-max`.
      type: string
      enum:
      - none
      - minimal
      - low
      - medium
      - high
      - xhigh
      nullable: true
    OpenAIResponseModalities:
      description: |-
        Output types that you would like the model to generate.
        Most models are capable of generating text, which is the default:
        `["text"]`
        The `gpt-4o-audio-preview` model can also be used to
        [generate audio](/docs/guides/audio). To request that this model generate
        both text and audio responses, you can use:
        `["text", "audio"]`
      type: array
      items:
        type: string
        enum:
        - text
        - audio
      nullable: true
    OpenAIStopConfiguration:
      oneOf:
      - type: string
      - type: array
        items:
          type: string
      description: |-
        Not supported with latest reasoning models `o3` and `o4-mini`.
        Up to 4 sequences where the API will stop generating further tokens. The
        returned text will not contain the stop sequence.
    OpenAIVerbosity:
      description: |-
        Constrains the verbosity of the model's response. Lower values will result in
        more concise responses, while higher values will result in more verbose responses.
        Currently supported values are `low`, `medium`, and `high`.
      type: string
      enum:
      - low
      - medium
      - high
      nullable: true
    OpenAIVoiceIdsOrCustomVoice:
      anyOf:
      - $ref: '#/components/schemas/OpenAIVoiceIdsShared'
      - type: object
        properties:
          id:
            type: string
        required:
        - id
      description: A built-in voice name or a custom voice reference.
      title: Voice
    OpenAIVoiceIdsShared:
      anyOf:
      - type: string
      - type: string
        enum:
        - alloy
        - ash
        - ballad
        - coral
        - echo
        - sage
        - shimmer
        - verse
        - marin
        - cedar
    ChatCompletionsBody:
      type: object
      properties:
        metadata:
          $ref: '#/components/schemas/OpenAIMetadata'
          nullable: true
        top_logprobs:
          type: integer
          nullable: true
        temperature:
          type: number
          nullable: true
        top_p:
          type: number
          nullable: true
        user:
          type: string
          description: |-
            A unique identifier representing your end-user, which can help to
            monitor and detect abuse.
          deprecated: true
        safety_identifier:
          type: string
          maxLength: 64
          description: |-
            A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
              The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
        prompt_cache_key:
          type: string
          description: Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching).
        prompt_cache_retention:
          type: string
          enum:
          - in_memory
          - 24h
          nullable: true
        messages:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionRequestMessage'
          minItems: 1
          description: |-
            A list of messages comprising the conversation so far. Depending on the
            model you use, different message types (modalities) are supported,
            like text, images, and audio.
        model:
          type: string
          description: |-
            Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI
              offers a wide range of models with different capabilities, performance
              characteristics, and price points. Refer to the [model guide](/docs/models)
              to browse and compare available models.
        modalities:
          $ref: '#/components/schemas/OpenAIResponseModalities'
        verbosity:
          $ref: '#/components/schemas/OpenAIVerbosity'
        reasoning_effort:
          $ref: '#/components/schemas/OpenAIReasoningEffort'
        max_completion_tokens:
          description: |-
            An upper bound for the number of tokens that can be generated for a
            completion, including visible output tokens and reasoning tokens.
          type: integer
          nullable: true
        frequency_penalty:
          minimum: -2
          maximum: 2
          description: |-
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
              their existing frequency in the text so far, decreasing the model's
              likelihood to repeat the same line verbatim.
          type: number
          nullable: true
        presence_penalty:
          minimum: -2
          maximum: 2
          description: |-
            Number between -2.0 and 2.0. Positive values penalize new tokens based on
              whether they appear in the text so far, increasing the model's likelihood
              to talk about new topics.
          type: number
          nullable: true
        response_format:
          allOf:
          - $ref: '#/components/schemas/OpenAICreateChatCompletionRequestResponseFormat'
          description: |-
            An object specifying the format that the model must output.

            Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
            Structured Outputs which ensures the model will match your supplied JSON
            schema.

            Setting to `{ "type": "json_object" }` enables JSON mode, which ensures
            the message the model generates is valid JSON.

            **Important:** when using JSON mode, you **must** also instruct the model
            to produce JSON yourself via a system or user message. Without this, the
            model may generate an unending stream of whitespace until the generation
            reaches the token limit, resulting in a long-running and seemingly "stuck"
            request. Also note that the message content may be partially cut off if
            `finish_reason="length"`, which indicates the generation exceeded
            `max_tokens` or the conversation exceeded the max context length.
        audio:
          description: |-
            Parameters for audio output. Required when audio output is requested with
            `modalities: ["audio"]`.
          $ref: '#/components/schemas/OpenAICreateChatCompletionRequestAudio'
          nullable: true
        store:
          description: |-
            Whether or not to store the output of this chat completion request for
            use in model distillation or evals products.
          type: boolean
          nullable: true
        stream:
          description: |-
            If set to true, the model response data will be streamed to the client
            as it is generated using server-sent events.
          type: boolean
          nullable: true
        stop:
          $ref: '#/components/schemas/OpenAIStopConfiguration'
          nullable: true
        logit_bias:
          description: |-
            Modify the likelihood of specified tokens appearing in the completion.
              Accepts a JSON object that maps tokens (specified by their token ID in the
              tokenizer) to an associated bias value from -100 to 100. Mathematically,
              the bias is added to the logits generated by the model prior to sampling.
              The exact effect will vary per model, but values between -1 and 1 should
              decrease or increase likelihood of selection; values like -100 or 100
              should result in a ban or exclusive selection of the relevant token.
          x-oaiTypeLabel: map
          type: object
          nullable: true
        logprobs:
          description: |-
            Whether to return log probabilities of the output tokens or not. If true,
              returns the log probabilities of each output token returned in the
              `content` of `message`.
          type: boolean
          nullable: true
        max_tokens:
          description: |-
            The maximum number of tokens that can be generated in the chat completion.
            This value can be used to control costs for text generated via API.

            This value is now deprecated in favor of `max_completion_tokens`, and is
            not compatible with o1 series models.
          deprecated: true
          type: integer
          nullable: true
        'n':
          minimum: 1
          maximum: 128
          description: How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.
          type: integer
          nullable: true
        prediction:
          description: |-
            Configuration for a predicted output, which can greatly improve
            response times when large parts of the model response are known
            ahead of time. This is most common when you are regenerating a
            file with only minor changes to most of the content.
          $ref: '#/components/schemas/OpenAIPredictionContent'
          nullable: true
        seed:
          description: |-
            This feature is in Beta.
              If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
              Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.
          deprecated: true
          x-oaiMeta:
            beta: true
          type: integer
          format: int64
          nullable: true
        stream_options:
          $ref: '#/components/schemas/OpenAIChatCompletionStreamOptions'
          nullable: true
        tools:
          type: array
          items:
            anyOf:
            - $ref: '#/components/schemas/OpenAIChatCompletionTool'
            - $ref: '#/components/schemas/OpenAICustomToolChatCompletions'
          description: |-
            A list of tools the model may call. You can provide either
              [custom tools](/docs/guides/function-calling#custom-tools) or
              [function tools](/docs/guides/function-calling).
        tool_choice:
          $ref: '#/components/schemas/OpenAIChatCompletionToolChoiceOption'
        parallel_tool_calls:
          type: boolean
          description: Whether to enable [parallel function calling](/docs/guides/function-calling#configuring-parallel-function-calling) during tool use.
        function_call:
          oneOf:
          - type: string
            enum:
            - none
            - auto
          - $ref: '#/components/schemas/OpenAIChatCompletionFunctionCallOption'
          description: |-
            Deprecated in favor of `tool_choice`.
              Controls which (if any) function is called by the model.
              `none` means the model will not call a function and instead generates a
              message.
              `auto` means the model can pick between generating a message or calling a
              function.
              Specifying a particular function via `{"name": "my_function"}` forces the
              model to call that function.
              `none` is the default when no functions are present. `auto` is the default
              if functions are present.
          deprecated: true
        functions:
          type: array
          items:
            $ref: '#/components/schemas/OpenAIChatCompletionFunctions'
          minItems: 1
          maxItems: 128
          description: |-
            Deprecated in favor of `tools`.
              A list of functions the model may generate JSON inputs for.
          deprecated: true
        user_security_context:
          $ref: '#/components/schemas/AzureUserSecurityContext'
      required:
      - messages
      - model
    InlineResponse200:
      anyOf:
      - type: object
        required:
        - id
        - choices
        - created
        - model
        - object
        properties:
          id:
            type: string
            description: A unique identifier for the chat completion.
          choices:
            type: array
            items:
              $ref: '#/components/schemas/OpenAICreateChatCompletionResponseChoices'
            description: A list of chat completion choices. Can be more than one if `n` is greater than 1.
          created:
            type: integer
            format: unixtime
            description: The Unix timestamp (in seconds) of when the chat completion was created.
          model:
            type: string
            description: The model used for the chat completion.
          system_fingerprint:
            type: string
            description: |-
              This fingerprint represents the backend configuration that the model runs with.
                Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
            deprecated: true
          object:
            type: string
            enum:
            - chat.completion
            description: The object type, which is always `chat.completion`.
            x-stainless-const: true
          usage:
            $ref: '#/components/schemas/OpenAICompletionUsage'
          prompt_filter_results:
            type: array
            items:
              $ref: '#/components/schemas/InlineResponse200PromptFilterResults'
      - type: object
        required:
        - id
        - choices
        - created
        - model
        - object
        properties:
          id:
            type: string
            description: A unique identifier for the chat completion. Each chunk has the same ID.
          choices:
            type: array
            items:
              $ref: '#/components/schemas/OpenAICreateChatCompletionStreamResponseChoices'
            description: |-
              A list of chat completion choices. Can contain more than one elements if `n` is greater than 1. Can also be empty for the
                last chunk if you set `stream_options: {"include_usage": true}`.
          created:
            type: integer
            format: unixtime
            description: The Unix timestamp (in seconds) of when the chat completion was created. Each chunk has the same timestamp.
          model:
            type: string
            description: The model to generate the completion.
          system_fingerprint:
            type: string
            description: |-
              This fingerprint represents the backend configuration that the model runs with.
                Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
            deprecated: true
          object:
            type: string
            enum:
            - chat.completion.chunk
            description: The object type, which is always `chat.completion.chunk`.
            x-stainless-const: true
          usage:
            allOf:
            - $ref: '#/components/schemas/OpenAICompletionUsage'
            description: |-
              An optional field that will only be present when you set
                `stream_options: {"include_usage": true}` in your request. When present, it
                contains a null value **except for the last chunk** which contains the
                token usage statistics for the entire request.
              *NOTE:** If the stream is interrupted or cancelled, you may not
                receive the final usage chunk which contains the total token usage for
                the request.
          delta:
            $ref: '#/components/schemas/OpenAIChatCompletionStreamResponseDelta'
          content_filter_results:
            $ref: '#/components/schemas/AzureContentFilterResultForChoice'
    InlineResponse200PromptFilterResults:
      type: object
      properties:
        prompt_index:
          type: integer
          format: int32
          description: The index of the input prompt that this content filter result corresponds to.
        content_filter_results:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterResultForPrompt'
          description: The content filter results associated with the indexed input prompt.
      required:
      - prompt_index
      - content_filter_results
    AzureContentFilterBlocklistResultDetails:
      type: object
      properties:
        filtered:
          type: boolean
          description: A value indicating whether the blocklist produced a filtering action.
        id:
          type: string
          description: The ID of the custom blocklist evaluated.
      required:
      - filtered
      - id
    AzureContentFilterCustomTopicResultDetails:
      type: object
      properties:
        detected:
          type: boolean
          description: A value indicating whether the topic is detected.
        id:
          type: string
          description: The ID of the custom topic evaluated.
      required:
      - detected
      - id
    AzureContentFilterResultForChoiceError:
      type: object
      properties:
        code:
          type: integer
          format: int32
          description: A distinct, machine-readable code associated with the error.
        message:
          type: string
          description: A human-readable message associated with the error.
      required:
      - code
      - message
      description: If present, details about an error that prevented content filtering from completing its evaluation.
    AzureContentFilterResultForChoiceProtectedMaterialCode:
      type: object
      properties:
        filtered:
          type: boolean
          description: Whether the content detection resulted in a content filtering action.
        detected:
          type: boolean
          description: Whether the labeled content category was detected in the content.
        citation:
          $ref: '#/components/schemas/AzureContentFilterResultForChoiceProtectedMaterialCodeCitation'
      required:
      - filtered
      - detected
      description: A detection result that describes a match against licensed code or other protected source material.
    AzureContentFilterResultForPromptContentFilterResults:
      type: object
      properties:
        sexual:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category for language related to anatomical organs and genitals, romantic relationships, acts
            portrayed in erotic or affectionate terms, pregnancy, physical sexual acts, including those portrayed as an
            assault or a forced sexual violent act against one's will, prostitution, pornography, and abuse.
        hate:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category that can refer to any content that attacks or uses pejorative or discriminatory
            language with reference to a person or identity group based on certain differentiating attributes of these groups
            including but not limited to race, ethnicity, nationality, gender identity and expression, sexual orientation,
            religion, immigration status, ability status, personal appearance, and body size.
        violence:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category for language related to physical actions intended to hurt, injure, damage, or kill
            someone or something; describes weapons, guns and related entities, such as manufactures, associations,
            legislation, and so on.
        self_harm:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterSeverityResult'
          description: |-
            A content filter category that describes language related to physical actions intended to purposely hurt, injure,
            damage one's body or kill oneself.
        profanity:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
          description: |-
            A detection result that identifies whether crude, vulgar, or otherwise objection language is present in the
            content.
        custom_blocklists:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterBlocklistResult'
          description: A collection of binary filtering outcomes for configured custom blocklists.
        custom_topics:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterCustomTopicResult'
          description: A collection of binary filtering outcomes for configured custom topics.
        error:
          $ref: '#/components/schemas/AzureContentFilterResultForChoiceError'
        jailbreak:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
          description: |-
            A detection result that describes user prompt injection attacks, where malicious users deliberately exploit
            system vulnerabilities to elicit unauthorized behavior from the LLM. This could lead to inappropriate content
            generation or violations of system-imposed restrictions.
        indirect_attack:
          allOf:
          - $ref: '#/components/schemas/AzureContentFilterDetectionResult'
          description: |-
            A detection result that describes attacks on systems powered by Generative AI models that can happen every time
            an application processes information that wasn’t directly authored by either the developer of the application or
            the user.
      required:
      - jailbreak
      - indirect_attack
      description: The content filter category details for the result.
    AzureContentFilterResultForChoiceProtectedMaterialCodeCitation:
      type: object
      properties:
        license:
          type: string
          description: The name or identifier of the license associated with the detection.
        URL:
          type: string
          format: uri
          description: The URL associated with the license.
      description: If available, the citation details describing the associated license and its location.
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: api-key
    OAuth2Auth:
      type: oauth2
      flows:
        implicit:
          authorizationUrl: https://login.microsoftonline.com/common/oauth2/v2.0/authorize
          scopes:
            https://cognitiveservices.azure.com/.default: ''
servers:
- url: '{endpoint}/openai/v1'
  description: Azure AI Foundry Models APIs
  variables:
    endpoint:
      default: ''
      description: |-
        A supported Azure AI Foundry Models APIs endpoint, including protocol and hostname.
        For example:
        https://westus.api.cognitive.microsoft.com).
